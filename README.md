# p5js with typescript and vite

Run `npm install`, then either `just run` or `npm run dev` to run the dev server

In order to build js file for openprocessing, run `just build` (requires `sd` to be installed)
